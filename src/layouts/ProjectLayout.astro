---
import BasicPageLayout from "./BasicPageLayout.astro";

const { frontmatter } = Astro.props;
---
<BasicPageLayout title={frontmatter.title}>
  <div class="prose">
    <h1>{frontmatter.title}</h1>
    <p>{frontmatter.date}</p>
    <p>{frontmatter.description}</p>
  </div>
</BasicPageLayout>
